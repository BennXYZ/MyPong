namespace Pong
{
    public enum GameState
    {
        None,
        Started,
        GameOver,
        CountDown,
        Searching
    }

    public enum ConnectionType
    {
        Server,
        Client
    }
}
